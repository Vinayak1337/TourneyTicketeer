const fs = require('fs');
const Discord = require('discord.js');

module.exports = (Bot, guild) => {
// return Bot.channels.cache.get('755702453197865022').send(new Discord.MessageEmbed().setDescription(`Rekoved from guild: ${guild.name}\nMembers: ${guild.members.cache.size}\nOwner: ${guild.owner.tag}`).setThumbnail(guild.iconURL({dynamic:true}).setColor('RED')));
}